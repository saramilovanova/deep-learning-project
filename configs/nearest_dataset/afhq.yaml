defaults:
  - /defaults.yaml

experiment:
  run_name: nearest_afhq
  tags: [baseline, nearest, afhq]

dataset:
  name: afhq
  split: train
  download: true
  batch_size: 256
  subset_size: null # Use full dataset for nearest dataset computation
  resolution: 64  # Resize to 64x64 for faster processing

model:
  name: nearest_dataset
  params: {}

metrics:
  baseline_path: "data/models/baseline_unet/afhq/ckpt_epoch_200.pt"
