defaults:
  - /defaults.yaml

experiment:
  run_name: optimal_cifar10
  tags: [baseline, optimal, cifar10]

dataset:
  name: cifar10
  split: train
  download: true
  batch_size: 512
  subset_size: null  # Use full dataset for optimal denoiser index

model:
  name: optimal
  params:
    temperature: 1.0  # Temperature for softmax weighting
    num_neighbors: 200  # Number of nearest neighbors to use for search

metrics:
  baseline_path: "data/models/baseline_unet/cifar10/ckpt_epoch_200.pt"

