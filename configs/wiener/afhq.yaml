defaults:
  - /defaults.yaml

experiment:
  run_name: wiener_afhq
  tags: [baseline, wiener, afhq]

dataset:
  name: afhq
  split: train
  download: true  # Manual download required
  batch_size: 256
  subset_size: null  # Use full dataset for Wiener filter computation
  resolution: 64  # Resize to 64x64 for manageable covariance matrix

model:
  name: wiener

metrics:
  baseline_path: "data/models/baseline_unet/afhq/ckpt_epoch_200.pt"
