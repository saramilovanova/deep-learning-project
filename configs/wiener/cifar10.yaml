defaults:
  - /defaults.yaml

experiment:
  run_name: wiener_cifar10
  tags: [baseline, wiener, cifar10]

dataset:
  name: cifar10
  split: train
  download: true
  batch_size: 512
  subset_size: null  # Use full dataset for Wiener filter computation

model:
  name: wiener

metrics:
  baseline_path: "data/models/baseline_unet/cifar10/ckpt_epoch_200.pt"

